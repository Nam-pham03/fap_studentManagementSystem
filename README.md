# fap_studentManagementSyste

A full-stack app for managing student data using React and Spring Boot.

## Setup
### Frontend
- Navigate to `frontend/`
- Run `npm install` and `npm start`

### Backend
- Navigate to `backend/`
- Run `mvn spring-boot:run`
